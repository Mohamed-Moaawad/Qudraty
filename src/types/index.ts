export type CustomButtonType = {
    text: string,
    icon: React.ReactNode,
    color: string,
    backgroundColor: string,
    url: string,
}

export type CustomContainerType = {
    children: React.ReactNode,
}

export type TextTitleType = {
    title: string,
    description?: string,
}

export type FeatureCardType = {
    icon: React.ReactNode,
    iconColor: string,
    title: string,
    description: string,
}